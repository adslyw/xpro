<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title,"标题" %>
    <%= f.text_field :title, :size => '121' %>
  </div>
  <div class="field">
    <br />
    <%= f.text_area :body,:size => "125x25", :class => "redactor" %>
  </div>
  <div class="actions">
    <%= f.submit :value => "更新", :class => "btn btn-info btn-sm" %>
    <%= link_to "返回", posts_path, :class => "btn btn-danger btn-sm" %>
  </div>
<% end %>
